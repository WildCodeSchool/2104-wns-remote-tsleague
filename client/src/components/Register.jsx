import { gql, useMutation } from '@apollo/client';

function Register() {
  return (
    <div>Regiter</div>
  );
}

export default Register;

// const USER_REGISTER = gql`
//   mutation register($body: String!){
//     register(body: )
//   }
// `
